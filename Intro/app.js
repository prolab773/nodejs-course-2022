// npm - global command, comes with node
// npm --version

// local deoendency - use it only in this particular project
// npm -i <packageName>

// global dependency - use it in any project
// npm install -g <packageName>
// sudo install -g <packageName> (mac)

